#include <iostream>
using namespace std;

int main() {
    int arr[] = {1,2,3};
    int n = 3, sum = 3;

    int count = 0;

    for(int i=0;i<n;i++) {
        int s=0;
        for(int j=i;j<n;j++) {
            s += arr[j];
            if(s==sum) count++;
        }
    }

    cout<<count;
}
